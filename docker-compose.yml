services:
  producer:
    build: ./producer-service
    ports:
      - "5000:5000"
    env_file:
      - ./producer-service/.env

  consumer:
    build: ./consumer-service
    ports:
      - "5001:5001"
    env_file:
      - ./consumer-service/.env
